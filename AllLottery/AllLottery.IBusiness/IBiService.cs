using AllLottery.Model;

namespace AllLottery.IBusiness
{
    //public interface IBiService
    //{
    //    string CreatePlatformUrl(int playerId, int platformId);

    //    void Transfer(int playerId, int platformId, decimal money);

    //    decimal CheckMoney(int playerId, int platformId);

    //    Platform[] GetAllPlatforms();
    //}
}