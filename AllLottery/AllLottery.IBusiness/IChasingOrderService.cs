using AllLottery.Model;
using AllLottery.ViewModel;

namespace AllLottery.IBusiness
{
    //public interface IChasingOrderService
    //{
    //    int AddOrder(int playerId, ChasingOrderAddOrderViewModel model);

    //    bool IsChasingBet(int betId);

    //    ChasingOrderDetail GetChasingOrderDetail(int betId);

    //    void CancleChasing(int orderId);

    //    ChasingOrder[] GetOrders(int playerId, int index, int size, out int total);

    //    ChasingOrder GetOrder(int orderId);
    //}
}